import 'package:flutter/material.dart';

class SelectedFoodPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Selected Food')),
    );
  }
}
